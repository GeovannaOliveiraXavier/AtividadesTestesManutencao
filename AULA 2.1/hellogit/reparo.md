# Git Commit Amend

O comando git commit --amend permite alterar o último commit realizado.

Ele pode ser utilizado para:

- alterar a mensagem do último commit;
- adicionar arquivos esquecidos;
- corrigir pequenos erros sem criar um novo commit.

Exemplo:

git commit --amend